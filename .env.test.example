# Integration Test Configuration
# Copy this file to .env to run integration tests

# Required: Your Azure DevOps organization URL
AZURE_DEVOPS_ORG_URL=https://dev.azure.com/your-organization

# Required: Personal Access Token with Analytics read permissions
# To create a PAT:
# 1. Go to Azure DevOps -> User Settings -> Personal Access Tokens
# 2. Create a new token with these scopes:
#    - Analytics (Read)
#    - Work Items (Read) 
#    - Project and Team (Read)
AZURE_DEVOPS_PAT=your-personal-access-token

# Optional: Default project for testing
# If not specified, tests will query across all projects
AZURE_DEVOPS_PROJECT=your-project-name

# Note: Integration tests will automatically skip if credentials are missing or invalid